# PaddleOCR (선택) — Python 3.10 ~ 3.12
# Windows PoC 권장: 2.7 라인 (3.x는 oneDNN 런타임 이슈가 있을 수 있음)
#
# 설치:
#   pip uninstall paddlepaddle paddleocr paddlex -y
#   pip install -r requirements-paddle.txt
#
# PaddleOCR 3.x + GPU → Linux Docker: requirements-paddle-v3.txt + Dockerfile.gpu
#   docker compose -f docker-compose.gpu.yml up --build

numpy>=1.26.0,<2.0
# GPU: paddlepaddle 대신 paddlepaddle-gpu==2.6.2 설치 (둘 중 하나만)
# paddlepaddle==2.6.2
opencv-contrib-python==4.6.0.66
opencv-python==4.6.0.66
paddleocr==2.7.3
