# PaddleOCR (선택) — Python 3.10 ~ 3.12 권장
# Python 3.14 등 최신 버전에서는 wheel이 없어 설치되지 않을 수 있습니다.
#
# Windows CPU 예시 (Python 3.12 가상환경에서):
#   pip install paddlepaddle==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
#   pip install paddleocr>=2.7.0
#
# Linux (Docker python:3.12-slim) 예시:
#   pip install paddlepaddle>=3.0.0
#   pip install paddleocr>=2.7.0

paddlepaddle>=3.0.0
paddleocr>=2.7.0
