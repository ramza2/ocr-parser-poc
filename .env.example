# VLM 워커 URL (backend 컨테이너 → 워커 :8001)
#
# GPU PC / GPU 서버가 따로 있을 때 (LAN IP):
# VLM_WORKER_URL=http://192.168.0.10:8001
#
# frontend + backend + vlm-worker 가 같은 Ubuntu GPU 서버일 때:
# VLM_WORKER_URL=http://host.docker.internal:8001
#
VLM_WORKER_URL=http://host.docker.internal:8001
